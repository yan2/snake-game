{
  "name": "snake-app",
  "version": "1.0.0",
  "description": "Classic snake game.",
  "private": true,
  "scripts": {
    "init": "mkdir dist",
    "clean": "rm -rf dist",
    "start": "npm run watch",
    "watch": "webpack -d --watch",
    "lint": "jshint **.js",
    "html": "cp src/index.html dist/index.html",
    "styles": "cp src/styles.css dist/styles.css",
    "webpack": "webpack",
    "build": "npm run clean && npm run init && npm run html && npm run styles && npm run webpack"
  },
  "author": "Ann Niou",
  "license": "",
  "dependencies": {},
  "devDependencies": {
    "jshint": "^2.9.5",
    "mocha": "^5.0.5",
    "webpack": "^4.2.0",
    "webpack-cli": "^2.0.13"
  }
}
